# Adapter Standard

Adapters connect Synthera capabilities to specific providers.

An adapter must declare its provider, supported capability, operations, permission needs, failure states, and version.

Agents must not depend on adapter-specific syntax.
