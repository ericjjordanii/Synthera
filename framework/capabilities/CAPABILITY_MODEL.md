# Synthera Capability Model

Status: Draft
Schema version: 1.0

Synthera agents depend on abstract capabilities rather than vendor-specific tools.

Core rule: agent logic requests a capability; an adapter implements that capability for the current provider.
