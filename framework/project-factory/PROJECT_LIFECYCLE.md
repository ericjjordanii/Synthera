# Synthera Project Lifecycle

A project is created from a manifest, inherits Synthera Core rules, receives project-specific agents and workflows, passes validation, and then becomes active.

Projects can later be paused, archived, migrated, or retired without changing Synthera Core.
